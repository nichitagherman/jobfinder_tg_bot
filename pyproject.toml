[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "jobfinder-tg-bot"
version = "0.1.0"
description = "Local-first Telegram bot for daily job digests."
requires-python = ">=3.13"

[tool.setuptools]
packages = ["jobfinder"]
